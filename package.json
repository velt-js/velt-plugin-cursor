{
  "name": "velt-plugin",
  "version": "1.0.3",
  "private": true,
  "description": "Cursor plugin for the Velt collaboration SDK",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-agent-skills.mjs",
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/deploy-skills.mjs",
    "validate": "node scripts/validate.mjs",
    "all": "npm run sync && npm run build && npm run deploy && npm run validate"
  },
  "author": {
    "name": "Velt",
    "email": "support@velt.dev"
  },
  "license": "MIT"
}
